console.log('<%= @checkoutAnon["id"] %>');

var stripe = Stripe('<%= ENV["stripeLivePublish"] %>', {stripeAccount: "<%= ENV['connectAccount'] %>"});

stripe.redirectToCheckout({
  	sessionId: '<%= @checkoutAnon["id"] %>'
 })
.then(function (result) {
})
.catch(function(error) {
  console.error('Error:', error);
});

